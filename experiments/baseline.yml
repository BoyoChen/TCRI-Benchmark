experiment_name: 'baseline'

model: ConvLSTM

data:
  data_folder: 'TCSA_data'
  batch_size: 10
  encode_length: 8
  estimate_distance: 8
  rotate_type: 'single'

training_setting:
  evaluate_freq: 10
  class_weight:
    - 1
    - 20
  learning_rate: 0.0005
  max_epoch: 100
 